const pessoa = {
    nome: "Neiva",
    altura: 1.78,
    idade: 66
};

pessoa.nome = "Clireidi"// podemos susbstituir o nome assim como a variável é substitutida 

console.log(`My name is ${pessoa.nome} and My years is ${pessoa.idade}`);