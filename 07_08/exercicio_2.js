let x = 10, y = 2;

let resultado = x + y;
mensagem = "A subtração de " + x + " e " + y + " é de " + resultado; // concatenação
console.log(mensagem)

resultado = x - y;
console.log(` A subtração de 10 - 2 é de: ${resultado}`);

resultado = x * y;
console.log(` A multiplicação de 10 * 2 é de: ${resultado}`);

resultado = x / y;
console.log(` A divisão de 10 por 2 é igual a :${resultado}`);